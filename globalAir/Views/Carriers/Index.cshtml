@model IEnumerable<ArcadePool.Models.Carrier>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CarrierName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CarrierName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CarrierID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CarrierID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CarrierID })
        </td>
    </tr>
}

</table>
